using VSI.EDGEAXConnector.AXAdapter.Controllers;
using VSI.EDGEAXConnector.Enums;
using VSI.EDGEAXConnector.ErpAdapter.Interface;

namespace VSI.EDGEAXConnector.AXAdapter
{
    public class AXAdapter 
    {
        //protected override void Load(ContainerBuilder builder)
        //{
        //    builder.Register(ad => new AXAdapter()).As<AXAdapter>();

        //    builder.Register((c, p) => new ProductController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IProductController>();
        //    builder.Register((c, p) => new CategoryController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<ICategoryController>();
        //    builder.Register((c, p) => new SaleOrderController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<ISaleOrderController>();
        //    builder.Register((c, p) => new UpdateSalesOrderController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IUpdateSalesOrderController>();
        //    builder.Register((c, p) => new InventoryController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IInventoryController>();
        //    builder.Register((c, p) => new PriceController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IPriceController>();
        //    builder.Register((c, p) => new CustomerController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<ICustomerController>();
        //    builder.Register((c, p) => new StoreLocationController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IStoreController>();
        //    builder.Register((c, p) => new PaymentController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IPaymentController>();
        //    builder.Register((c, p) => new ShippingController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IShippingController>();
        //    builder.Register((c, p) => new GiftCardController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IGiftCardController>();
        //    builder.Register((c, p) => new ChannelPublishingController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IChannelPublishingController>();
        //    builder.Register((c, p) => new WishListController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IWishListController>();
        //    builder.Register((c, p) => new ContactPersonController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IContactPersonController>();
        //    builder.Register((c, p) => new QuotationController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IQuotationController>();
        //    builder.Register((c, p) => new ChannelConfigurationController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IChannelConfigurationController>();
        //    builder.Register((c, p) => new OfferTypeGroupsController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IOfferTypeGroupController>();
        //    builder.Register((c, p) => new CartController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<ICartController>();
        //    builder.Register((c, p) => new DQSController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IDQSController>();
        //    builder.Register((c, p) => new DiscountController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IDiscountController>();
        //    builder.Register((c, p) => new DiscountWithAffiliationController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IDiscountWithAffiliationController>();
        //    builder.Register((c, p) => new QuantityDiscountController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IQuantityDiscountController>();
        //    builder.Register((c, p) => new QuantityDiscountWithAffiliationController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IQuantityDiscountWithAffiliationController>();
        //    builder.Register((c, p) => new InAppPurchaseController(p.Named<string>(CURRENTSTORE.storeKey.ToString()))).As<IInAppPurchaseController>();
        //}
    }


}